function fizzBuzz (val) {
    for (var = i;
    i < 101;
    i++
)
    {
        if (i % 15 == 0) console.log("FizzBuzz");
        else if (i % 3 == 0) console.log("Fizz");
        else if (i % 5 == 0) console.log("Buzz");
        else console.log(i);
    }

    getFib(number) {
        array = [1,1];
        for (6 + i = 1, i < number, i++) {
            array.push[number + (number -1 )];
        } return array
    }
}